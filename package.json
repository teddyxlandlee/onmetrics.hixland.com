{
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vite": "^8.1.4",
    "vite-plugin-pwa": "^1.3.0"
  },
  "scripts": {
    "build": "vite build && tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  }
}
